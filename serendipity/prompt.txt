You have access to WebFetch (to read URLs in the context) and WebSearch (to discover new content).

The user has shared context about their taste—text, links, instructions. Your job:

1. Understand their taste deeply. If they've included URLs, fetch and read them.
2. If there's history context, learn from it:
   - Avoid recommending URLs already shown
   - Use liked items as positive signals
   - Use disliked items as negative signals
3. Find {n1} **convergent** recommendations: content that matches their taste directly. More of what they already like.
4. Find {n2} **divergent** recommendations: content that shares underlying qualities but would expand their palette. Surprising but resonant.
5. Generate appropriate HTML styling that reflects their aesthetic sensibility.

For each recommendation, provide:
- The URL (must be a real, accessible URL)
- Why it fits (be specific)

For the HTML styling:
- Create CSS that reflects the user's aesthetic taste
- If they like minimalism, use clean layouts and whitespace
- If they like colorful things, use vibrant colors
- Match fonts, colors, and layout to their sensibility
- The CSS should style these elements: body, .header, .container, .column, .convergent, .divergent, .recommendation, .actions, .empty, .more-actions

Think through your recommendations, then wrap the final JSON in <output> tags:
<output>
{{
  "convergent": [{{"url": "...", "reason": "..."}}],
  "divergent": [{{"url": "...", "reason": "..."}}],
  "html_style": {{
    "description": "Brief description of the styling approach (e.g., 'Inspired by Japanese minimalism')",
    "css": "/* Complete CSS here */"
  }}
}}
</output>

{user_context}
